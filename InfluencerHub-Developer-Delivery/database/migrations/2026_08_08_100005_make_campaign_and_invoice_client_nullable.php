<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

/**
 * حملة العلامة وفاتورتها بلا عميل.
 *
 * قرار الملكية جعل العلامة كيانًا مستقلًّا في مستأجرها، و`brands.client_id`
 * صار اختياريًّا. لكنّ `campaigns.client_id` و`invoices.client_id` بقيا
 * إلزاميّين — والنتيجة أن مساحة العلامة **لا تستطيع إنشاء حملة واحدة**.
 * كشف ذلك اختبارٌ حاول إنشاء حملة لعلامة تملك نفسها فاصطدم بـNOT NULL.
 *
 * والمخرج الوحيد المتّسق مع القرار هو هذا: العلامة التي تشتري لنفسها ليس لها
 * «عميل» — هي جهة الطلب نفسها. والبديل المرفوض صراحةً هو اختلاق عميل يحمل
 * اسمها ليملأ العمود، وهو ما يخلط العميل بالعلامة ويشوّه التقارير والفوترة.
 *
 * ## أمان الهجرة
 *
 * NOT NULL ← NULL توسعةٌ لا تفشل ولا تفقد صفًّا: كل السجلّات القائمة تحمل
 * عميلًا وتبقى كما هي. ولا يتغيّر سلوك مسار الوكالة إطلاقًا — العمود يبقى
 * مطلوبًا في التحقّق هناك، وإنّما يُسمح بغيابه في مسار العلامة وحده.
 *
 * ## ما يجب أن ينتبه له القارئ
 *
 * كل كود يقرأ `$campaign->client->…` بلا فحصٍ لـnull صار عرضةً للسقوط متى
 * مرّت عليه حملةُ علامة. المسارات القائمة تقرأ حملات الوكالة وحدها فلا تتأثّر،
 * ومساحة العلامة تتعامل مع الغياب صراحةً.
 */
return new class extends Migration
{
    public function up(): void
    {
        Schema::table('campaigns', function (Blueprint $t) {
            $t->foreignId('client_id')->nullable()->change();
        });

        Schema::table('invoices', function (Blueprint $t) {
            $t->foreignId('client_id')->nullable()->change();
        });
    }

    /**
     * لا رجوع.
     *
     * إعادة NOT NULL تفشل متى وُجد صفٌّ بلا عميل — وهو الصفّ المشروع الذي
     * أُضيف العمودُ الفارغ من أجله. والتراجع الصامت هنا يعني حذف حملات علامات
     * قائمة، فيُترك صريحًا بدل أن يُخفى.
     */
    public function down(): void
    {
        // لا شيء عمدًا — انظر التعليق أعلاه.
    }
};
