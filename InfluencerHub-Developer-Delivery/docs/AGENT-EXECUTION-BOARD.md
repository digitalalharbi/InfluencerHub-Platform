# لوحة تنفيذ الوكلاء — InfluencerHub

آخر تحديث: 2026-07-19 · القائد: الوكيل 1 · الفرع: `main` · آخر Commit: `a1d51e5`

## قواعد الملكية

- **الوكيل 1 وحده يدمج على `main`.**
- لا يعدّل وكيلان الملف نفسه في الوقت نفسه — الملكية أدناه ملزمة.
- **مجموعة اختبار واحدة في كل مرّة** (deadlock على `influencerhub_testing`).
- وكيل يكتشف عيبًا خارج نطاقه: يسجّله في «العيوب المرصودة» ولا يعدّل ملفات غيره.
- بعد كل دمج: `php artisan test` كاملة + `npx tsc --noEmit`.

## الحالة الحالية

البيئة تُنفّذ الأدوار **بالتتابع** لا بالتوازي الكامل: العمل مترابط الحالة
(رحلة واحدة في قاعدة واحدة) والاختبارات لا تحتمل التزامن. الدور الوحيد الذي
يصلح للتوازي الآمن هو التدقيق للقراءة، لأنه لا يلمس كود التطبيق.

| # | الوكيل | الحالة | الملفات المملوكة | الاختبارات | Commit |
|---|---|---|---|---|---|
| 1 | القيادة والتنسيق | جارٍ | `docs/*` | — | `a1d51e5` |
| 2 | Laravel والخلفية | جارٍ | `app/Domain/**`, `app/Http/**`, `routes/**`, `database/**` | `php artisan test` | `0552e8d` `dfb8049` `a1d51e5` |
| 3 | التصميم والتجربة | لم يبدأ | `resources/css/**`, `resources/js/Layouts/**`, `resources/js/Components/**` | لقطات + overflow | — |
| 4 | تبسيط الرحلات | جارٍ (ضمن 2) | `resources/js/Pages/**` | Inertia tests | `dfb8049` |
| 5 | اختبار الرحلات والجودة | جارٍ (متصفّح) | `tests/**` | E2E + Feature | ضمن 2 |
| 6 | التكاملات والناشرون | لم يبدأ | `app/Domain/Platforms/**`, `config/platforms*` | — | — |
| 7 | SaaS والاشتراكات | لم يبدأ | `app/Domain/Billing/**` | — | — |
| 8 | الأداء والأمن والتسليم | لم يبدأ | `scripts/**`, `docs/PRODUCTION-*` | perf:audit | — |

## المهمة الجارية

**الرحلة التنفيذية للحملة 148** (الوكيل 2 + 5 بالتتابع).

منجز: قرار العميل → التعاون → القبول → إصدار العقد.
التالي: **توقيع العقد** من بوابتَي المبدع والعميل.

## العيوب المرصودة (خارج نطاق مكتشفها)

| العيب | المكتشف | المالك | الحالة |
|---|---|---|---|
| **لا مخزَن لإثبات النشر ولا للنتائج** — يحجب خطوتين من الرحلة | 5 | 2 | **مفتوح — يحتاج قرارًا: يدوي أم من تكامل** |
| 166 من 168 مبدعًا بلا حساب دخول — لا مسار دعوة إطلاقًا | 5 | 2 | **مفتوح — أكبر بند** |
| نمط متكرّر: حارس موجود في المخطّط/الواجهة وغير مُنفَّذ على الخادم | 1 | 2 | مفتوح — يحتاج تدقيقًا مخصّصًا |
| `/admin` غير مبنيّ — لوحة النظام على `/beta/admin` وحدها | 1 | 2 | مفتوح |
| البذور تملأ روابط لا ينتجها مسار المنتَج، فتُخفي العيب | 1 | 2 | جزئيًّا (العقود أُصلحت) |
| `TenantScope` مغلق افتراضيًا: فحص خارج السياق يمرّ كأن لا تعارض | 2 | 2 | أُصلح في العقود · يحتاج مسحًا |
| درجة المطابقة 0 وبلا أسباب للمبدعة 968 (`primary_platform` فارغ) | 5 | 6 | مفتوح |

## الاعتماديات الخارجية (موثّقة لا مُدّعاة)

| البند | الحالة الصادقة |
|---|---|
| منصّات التواصل (Snapchat/TikTok/Meta/YouTube/X/LinkedIn) | `waiting_for_credentials` |
| Google (Drive/Ads/Analytics/Login) | غير مبنيّ |
| مزوّد مدفوعات | غير مربوط — لا Mada ولا Apple Pay قبل مزوّد فعلي |
| SMS · SMTP | `waiting_for_credentials` |
| خطوط Smart Code المرجعية | يجب التحقّق من وجود ملفات المصدر قبل أي اعتماد |
